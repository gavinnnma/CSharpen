# .NET build output
bin/
obj/

# Rider / VS
.idea/
*.user

# OS
.DS_Store
Thumbs.db

# .csharpen/ holds your blank templates and manual saves — intentionally NOT ignored,
# so progress travels with the project across machines.
# EXCEPT passing/ : it stores solved answers, kept local so they don't ship in the repo.
.csharpen/passing/

# Obsidian vault memory symlink (not committed)
.memory
